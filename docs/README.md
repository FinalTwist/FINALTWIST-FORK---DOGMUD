# DOGMud documentation

Everything that isn't code. Start with [`world.md`](world.md) if you want the
setting, or [`schemas/`](schemas/) if you want to author content.

## Start here

| Path | What's in it |
|------|--------------|
| [`world.md`](world.md) | The world-design document — lore, factions, zones, species |
| [`PATCH_NOTES.md`](PATCH_NOTES.md) | Dated shipping log of every change |
| [`PRE_DEPLOY_PLAYTEST_CRIBSHEET.md`](PRE_DEPLOY_PLAYTEST_CRIBSHEET.md) | The owner's manual checklist before deploying the unified-resolution arc; the Elemental Queen fight is mandatory |
| [`U11_OWNER_PUNCHLIST.md`](U11_OWNER_PUNCHLIST.md) | Short companion to the crib sheet: only what U11 changed and did NOT verify, plus the decisions the owner has to make. Point-in-time, 2026-08-30 |
| [`PATH_TO_1.0.md`](PATH_TO_1.0.md) | Remaining work before the 1.0 tag |

## Reference

| Path | What's in it |
|------|--------------|
| [`schemas/`](schemas/) | YAML schema references (room, mob, item, spell, condition, dialogue, schedule, patrol) |
| [`architecture/`](architecture/) | System-level architecture notes and deliberate divergences from upstream |
| [`economy/`](economy/) | Living-economy design and tuning |
| [`balance/`](balance/) | Combat and progression tuning |
| [`worldbuilding/`](worldbuilding/) | Zone expansion plan, coordinate map, settlement canon, world atlas |

## Guides

| Path | What's in it |
|------|--------------|
| [`guides/CONTENT_GENERATION_GUIDE.md`](guides/CONTENT_GENERATION_GUIDE.md) | How the content-generation workflow fits together |
| [`guides/github_guide.md`](guides/github_guide.md) | Branch strategy and Git workflow |
| [`guides/DEPLOYMENT_GUIDE.md`](guides/DEPLOYMENT_GUIDE.md) | Deploying to the production droplet |
| [`guides/ADVERTISING_LISTINGS.md`](guides/ADVERTISING_LISTINGS.md) | Copy used for MUD-listing sites |
| [`guides/TESTING_GUIDE.md`](guides/TESTING_GUIDE.md) | Reproducible race-enabled test baseline, local and CI |

## Audits & findings

Point-in-time reports. Each is true as of its date and is **not** kept current —
verify against the code before acting on one.

| Path | What's in it |
|------|--------------|
| [`audits/`](audits/) | Tech-debt and test-coverage audits, the code-smell review queue, upstream cherry-pick triage, playtest findings |
| [`playtest_feedback.txt`](playtest_feedback.txt) | The owner's living backlog of findings from playing PRODUCTION, opened 2026-09-16 after the 2026-09-07 deploy (`59e1871fd`). Read it knowing prod trails master by roughly 30 PRs, so an item may already behave differently on master and is verified there before being worked. Items are marked as they land and are never deleted, so a disproven report cannot come back as new; the sealed-phial storage item is already marked resolved-not-a-bug after the owner retested it. Bugs, balance knobs, content gaps and feature requests are mixed together by design, because that is how play surfaces them; triage assigns each to an existing arc or files it as new work |
| [`superpowers/audits/messaging-m6-content-ledger.md`](superpowers/audits/messaging-m6-content-ledger.md) | Standing ledger of player-facing text the messaging unification arc has deferred to M6 (or M5, or "filed" with no stage named), collected across every slice's spec, plan and playtest memory so M6 does not have to rediscover them. Grouped by store: conditions, defence/taunt/casting, crafting, spells, quests, combat-messages/Group C, and prerequisites such as the ungranted `NightVision` buff blocking the M5 crime-in-the-dark ruling. Rows are never deleted, only marked done with a PR number |
| [`superpowers/audits/2026-09-20-melee-defence-band-shares.md`](superpowers/audits/2026-09-20-melee-defence-band-shares.md) | Measures how often a melee defensive win lands in each narration band (weak, normal, heavy) under the old self-relative z-score rule versus the new crit-plus-margin rule, across five attacker/defender matchups, 50,000 contest rolls each, via `internal/combat.RunContest` directly (`tools/melee_band_census`). Headline: a dominant defender goes from 69.0% weak / 2.3% heavy under the old rule to 0.3% weak / 87.4% heavy under the new one; an outclassed defender's rare wins go from 59.8% weak to 95.3% weak because the new rule's crit gate zeroes the margin on floor-granted wins, which the old rule has no equivalent check for; the even matchup barely moves. Measurement only, taken before M4c changed any production code |
| [`audits/META_REVIEW_AGENT_WORK_2026-08-08.md`](audits/META_REVIEW_AGENT_WORK_2026-08-08.md) | Adversarial meta-review grading the Cursor and Jules agent work after 2026-08-04 |
| [`audits/ADVERSARIAL_REVIEW.md`](audits/ADVERSARIAL_REVIEW.md) | Jules' architectural teardown: global lock, autosave I/O, boot wiring, progression math |
| [`audits/ADVERSARIAL_CODE_REVIEW_2026-08-07.md`](audits/ADVERSARIAL_CODE_REVIEW_2026-08-07.md) | Fresh-eyes review of server, commands, persistence, loaders, CI and client: 9 high, 18 medium, 5 low findings |
| [`audits/2026-08-19-progression-firing-audit.md`](audits/2026-08-19-progression-firing-audit.md) | Every progression call site with its firing condition. Ten different conventions found (the plan estimated seven); input to U10b |
| [`audits/2026-08-30-kinetic-backlash-reflect.md`](audits/2026-08-30-kinetic-backlash-reflect.md) | Where "An unseen force slams into you" comes from: condition 109 via the Kinetic Backlash mutation. Two defects, both open: the reflect has no death guard so a corpse hits back, and the copy names no source |
| [`audits/2026-08-30-shield-spells-converge-at-the-cap.md`](audits/2026-08-30-shield-spells-converge-at-the-cap.md) | Why Chrysalis Cocoon and Conviction Ward feel identical: shield magnitude is an unbounded percentage feeding a 75% cap, so every shield saturates past ~Wil 100 / skill 25. For the spell scaling arc |
| [`audits/2026-09-22-ai-companion-patch-evaluation.md`](audits/2026-09-22-ai-companion-patch-evaluation.md) | Evaluation of a third-party GoMud Discord contribution adding OpenAI-driven companion NPCs, plus the salvage list for the behaviour rework arc. Nothing adopted. Verdict: careful engineering and a small additive engine footprint, but the shipped defaults auto-bond every player with no consent step and the daily token budget lives only in process memory, so a restart resets it. Most of the value needs no model at all: the plan-versus-reflex combat split answers the arc's targeting race, `internal/goals` already computes `priority x archetypeWeight x contextMod` with hysteresis so the deterministic decider is built, and the Dijkstra walked-only worldmap, the bounded 23-verb action vocabulary and `sanitizeDecision` are all model-free. Also records why Jev cannot be self-hosted and why the trigger rate, not the unit price, sets any API bill |
| [`audits/2026-09-23-ai-companion-pr161-review.md`](audits/2026-09-23-ai-companion-pr161-review.md) | Review of PR #161, FinalTwist's cross-repo pull request landing the AI companion module as 71 files and +19,645 lines. Thirteen findings ranked by severity, each with fix options and their trade-offs. Headline: three defects that a green test suite could not see, because coverage is 28.3% and the entire apply path is at zero. A missing `defer` at `runtime.go:1143` lets a panic in the tool path leave the mud lock held, and the goroutine's own recover then re-takes it, freezing the world in a process that never exits; `speakInChunks` slices a rune array with a byte offset so any multibyte script panics; and because `applied` is set after `applyResult` returns while `settleTokens` runs inside it, a panic refunds the reservation twice until the daily budget stops binding. Separately the PR's claim that the `ask.go` change is a byte-for-byte refactor is false: the behaviour-tree early return moved from `Ask` into the extracted helper, so twelve mobs now leak "You hear someone talking." to adjacent rooms with the module switched OFF. Also records that any player can drive model calls through a stranger's companion, that a world event sharing a stimulus batch unlocks the owner-only verbs, and that `combat.go` reads `HealthMax.Value` where `internal/behaviortree` had already written a comment naming reserving gear on a companion as the exact reason to use `EffectivePoolMax`. Confirms nothing personal or secret is exposed in the diff. Carries the owner's eleven decisions taken on the strength of it, so it reads as a work plan rather than a list of open questions: the module IS being taken, but merging waits for graded lighting plan 6 because perception.go reads the sight model that arc is replacing; ask.go ships now as its own PR; AutoBond stays on but the first meeting becomes a consent prompt; non-owner asks are throttled rather than banned; give is restricted to the owner; moderation goes on by default with the fail-open fixed; and an instance-keyed opinion store becomes its own engine task. Four config calls are handed to FinalTwist rather than guessed at, three on cost and one on whether his character forgives; records that the engine's opinion decay is NOT a half-life despite the name but an integer-step pull of one point per DispositionDecayHalfLifeRounds, which at the shipped 100000 and four-second rounds is one point per 4.6 days, so "every other NPC forgives" is a far weaker claim than it sounds, and that a zero value already disables decay so permanence needs no new code. Every remedy carries its trade-offs, including the two tempting one-line fixes for the double refund that are each a different regression |
| [`superpowers/audits/2026-09-09-phase-2-closing.md`](superpowers/audits/2026-09-09-phase-2-closing.md) | Closing gate for the CLAUDE.md context-budget cut (1,094 to 84 lines): all 44 original sections traced to a verified current home with the grep used to check each, before/after sizes for CLAUDE.md, MEMORY.md and six context.md files, six defects found in CLAUDE.md's own text (two phantom functions, stale Go-default penalty numbers, an unreachable mutation tier, stale knob counts), six dead config knobs (Salvage and barter) whose call sites hard-code literals instead of reading them, and the memory hygiene items handed off to Phase 3 |
| [`perf/`](perf/) | Performance baselines and profiling notes |

Two of these are **live scripts**, not point-in-time reports — run them rather
than reading an old result:

| Path | What it does |
|------|--------------|
| [`../tools/context_md_audit.py`](../tools/context_md_audit.py) | Finds `context.md` files documenting symbols the package no longer defines. Coverage is 100%; accuracy is what rots. Triage the output, it has known false positives |
| [`../tools/lighting_golden_diff.py`](../tools/lighting_golden_diff.py) | Read-only analyser for the graded-lighting arc: groups a `lighting_daycycle.golden` diff by sample section and by biome transition, and reports section and room COUNT changes separately from value moves. Every golden move in the arc must be explained room-for-room with this before it is re-recorded |
| [`../tools/balance/`](../tools/balance/) | Combat balance models behind roadmap 5.11 — skill leverage, real player-vs-mob matchups, and the `SkillWeight`/statpool tuning matrix |
| [`../tools/combat_message_pool_audit.py`](../tools/combat_message_pool_audit.py) | Read-only check that every combat-message role pool is equal per tier, which is what the coordinated narration index of M3 item 8 requires. Exits 1 with a per-group listing when any pool is short |
| [`../tools/combat_message_pad_check.py`](../tools/combat_message_pad_check.py) | Read-only proof that a combat-message pad deleted and edited nothing, comparing each (file, verb, split, role, tier) group's multiset of lines against a baseline git ref. Permits free reordering and additions, which a plain diff cannot distinguish from an edit. Both directions were probed before it was trusted |
| [`../tools/fold_mob_training_to_base.py`](../tools/fold_mob_training_to_base.py) | One-shot U10b-0 Phase A migration that folded authored mob `training:` into `base:` across 599 templates, kept as the record of the arithmetic. `--verify <report.csv>` re-checks it against the files on disk |
| [`../tools/webclient-tests/`](../tools/webclient-tests/) | Headless, zero-dependency Node regression tests for the web client. No CI wiring (the client has no JS test infrastructure), run each by hand: `vitals-rows.js` (party vitals rows keyed on identity, not label, so same-named companions don't collide), `char-handler-shadowing.js` (guards the GMCP handler-shadowing trap where a generic push runs only the first matching handler), `quest-panel-refresh.js` (the Quests panel refreshes on a full `Char` push, not only `Char.Quests`), `zone-crossing-center.js` (the map keeps a current room, and so keeps drawing the quest arrow, across a zone-crossing step), `trigger-status-key-match.js` (a saved "status" trigger matches the `Char.Conditions` map key with its `#n` suffix stripped, never the display label that carries a stack count) |
| [`../tools/messaging_token_rewrite.py`](../tools/messaging_token_rewrite.py) | M4a text-line rewrite of shipped narration name tokens to the canonical `{actor}`/`{actee}`/`{actor_plain}`/`{actee_plain}` vocabulary, per-store table because the same spelling means a different role in different stores (conditions' `{source}` is the holder, which is the actee). Never `yaml.load`/`yaml.dump`, writes `<file>.tmp` then `os.replace`. `--group {kindb,items,grapple,position} --dry-run` previews, `--group ... ` rewrites, `--check` exits non-zero if any old token remains. Dry-run only so far; per-store rewrites land in later M4a tasks |
| [`../tools/messaging_role_key_check.py`](../tools/messaging_role_key_check.py) | M4b-1 proof that a role-key rename touched only golden labels. Byte-identity cannot show this, because a rename legitimately changes the labels a golden records. Reads each pre-rename golden from a base git ref, applies the same per-store label translation the rename applied, and demands equality with the working-tree golden; a swapped role still shows as a diff. `--base <ref> --group {combat,grapple,kindb,position}` |
| [`../tools/counter_pool_rekey_check.py`](../tools/counter_pool_rekey_check.py) | Counters-slice proof that re-keying the counter pools by the defence that won changed only what it should: dodge rows are the old melee rows byte for byte, ranged rows are gone, parry and block rows are new, defy rows may be re-toned, and every other row is unchanged. Reads the base golden blob and decodes it explicitly, never `text=True`, so the Windows locale codec cannot mojibake a non-ASCII character into a false difference. Also asserts a row census per pool so a golden that failed to regenerate cannot pass by comparing nothing, and a floor on the base row count so an unparsed or wrong-ref base cannot pass either. Sabotaged three ways before it was trusted: a dodge line edited by one letter, a non-counter line edited by one letter, and the base-row floor pushed above the real count so the real base fails it. `--base <ref>` |
| [`../tools/spell_axes_rewrite.py`](../tools/spell_axes_rewrite.py) | M4b-2 rewrite of spell YAML and Go test fixtures from the legacy `type:`/`target_defense_type:` pair to the three `combatvocab` axes (`attack_type`/`damage_type`/`targeting`). Never `yaml.load`/`yaml.dump`, writes `<file>.tmp` then `os.replace`. `--add` inserts the three derived keys beside the legacy ones (idempotent), `--strip` deletes the legacy `type:`/`target_defense_type:` lines, `--check` exits non-zero if any spell file is missing a new key or still carries a legacy one, `--go-tests` rewrites `_test.go` fixtures that build `SpellData` from the retiring fields; `--dry-run` previews any mode |

## History

| Path | What's in it |
|------|--------------|
| [`roadmaps/`](roadmaps/) | Long-form roadmaps: development plan, combat-state, mob-aliveness |
| [`roadmaps/ADVERSARIAL_REVIEW_REMEDIATION_ROADMAP.md`](roadmaps/ADVERSARIAL_REVIEW_REMEDIATION_ROADMAP.md) | 37-finding remediation roadmap from the 2026-08-07 adversarial reviews. Note: its source doc `ADVERSARIAL_CODE_REVIEW_2026-08-07.md` is currently untracked in the repo root |
| [`roadmaps/UNIFIED_RESOLUTION_ROADMAP.md`](roadmaps/UNIFIED_RESOLUTION_ROADMAP.md) | **CLOSED.** Stages U0–U12 collapsing 34 scattered opposed-roll resolution sites onto one contest core, plus the cost and harm model. Refactor-first, one behaviour flip at U6. Shipped to production 2026-08-30; kept as the arc's merge-evidence record |
| [`superpowers/specs/completed/2026-08-31-toxicity-tolerance-and-visibility-design.md`](superpowers/specs/completed/2026-08-31-toxicity-tolerance-and-visibility-design.md) | Toxicity redesign: tolerance earned from the alchemy skill rather than a flat ceiling, nine untagged legacy potions backfilled, and two feedback bands added below the first penalty band so pressure is visible before it bites |
| [`superpowers/plans/completed/2026-08-31-toxicity-tolerance-and-visibility.md`](superpowers/plans/completed/2026-08-31-toxicity-tolerance-and-visibility.md) | Implementation plan for the toxicity spec above, in 8 tasks. Corrects four of the spec's facts: Meirok's vitality is read as `ValueAdj` (150 with gear, not the save's `base: 104`), which retunes `ToxicityAlchemyScale` to 2.5; `status` and `help toxicity` already work; and the new ceiling would have made Chrysalis Catalyst and Mutagen Brew undrinkable, so the heavy tier is rescaled |
| [`superpowers/specs/2026-08-31-messaging-unification-design.md`](superpowers/specs/2026-08-31-messaging-unification-design.md) | Design for the messaging unification arc: 14 narration stores, 6 role conventions, 2 token engines and 3 send helpers collapsed onto one Actor/Actee/Observer core. Same refactor-first shape as the resolution arc, guarded by a snapshot harness that freezes today's flavor |
| [`superpowers/plans/completed/2026-09-07-messaging-m1-viewpoint-audit-and-harness.md`](superpowers/plans/completed/2026-09-07-messaging-m1-viewpoint-audit-and-harness.md) | Implementation plan for M1 of the arc above, in 5 tasks. Task 1 is the deliverable: a ruled audit of all 247 hand-rolled narration sites saying whether each missing viewpoint is correct (the actee is a mob, or the exchange is private) or a gap (a public act the room is never told about). Records that the scan finding those sites produces false positives, and that `messaging.Category` cannot classify them — `give.go` sends one event across two categories |
| [`superpowers/specs/completed/2026-09-08-messaging-m2-shared-narration-seam-design.md`](superpowers/specs/completed/2026-09-08-messaging-m2-shared-narration-seam-design.md) | Design for M2 of the arc above: one `SendTrio` seam in `internal/messaging` that fans a narrated event out to actor, actee and observer, with all 23 special-move verb files on both the player and mob side migrated onto it and seven output changes: the five defects M1 found across seven sites, a detail-line ruling, and an information leak where a player defending in the dark is told who hit them if the attacker was a player but not if it was a mob. Records the import edges that rule out `internal/narration` and `internal/actions` as homes, the finding that the three roles do not share a `messaging.Category` (so the category rides on the line, or `shoot` and `throw` would be silently recategorised), and supersedes the arc spec's M2 section in two places: M1's goldens cannot protect this refactor, so a literal freeze is built first, and M2 ends with the playtest gate because it authors player-facing lines |
| [`superpowers/plans/completed/2026-09-08-messaging-m2-shared-narration-seam.md`](superpowers/plans/completed/2026-09-08-messaging-m2-shared-narration-seam.md) | Implementation plan for the M2 spec above, in 14 tasks. Task 0 builds the net first: a literal freeze over the 23 migration files, because M1's goldens snapshot the message stores and do not cover a single line of hand-rolled `fmt.Sprintf` text. Then the seam, both defence helpers, 23 one-file-per-commit migrations, and the seven output changes held to the end so the refactor and the behavior changes never share a diff |
| [`superpowers/specs/completed/2026-09-09-messaging-m3-store-core-extraction-design.md`](superpowers/specs/completed/2026-09-09-messaging-m3-store-core-extraction-design.md) | Design for M3 items 1 and 2 of the arc above: a rendering core in `internal/narration` that takes one `Variants` per role, picks a single coordinated index and substitutes tokens, with defence, itemvoices and casting migrated onto it. Records that the import graph rules out `internal/items` as the home, because `items` imports `conditions` and M3 item 5 migrates conditions, while `narration` imports only `util` and is already imported by five of the nine store owners. Finds that the arc's nine stores are really two kinds: six hold pools, but conditions, spells, quests and crafting hold single strings and have no selection problem at all, so the unifying concept is the role set plus a selector rather than a pool renderer. Adopts the assembly rule (the store computes bands, tier unions and cooldown filters; the core only coordinates the index), which keeps M4 a parameter flip, and gives `Roles` its fourth field now for `combat-messages`' `separate` case where attacker and defender are in different rooms. The goldens are required byte-identical and explicitly not re-recorded, because `defense_messages.golden` keys rows by the authored role name and is therefore the only thing that can catch the role swap this refactor makes easy |
| [`superpowers/plans/completed/2026-09-09-messaging-m3-store-core-extraction.md`](superpowers/plans/completed/2026-09-09-messaging-m3-store-core-extraction.md) | Implementation plan for the M3 spec above, in 8 tasks. Task 0 builds the net first, as every slice of this arc has had to: two pre-existing failures make `go test ./...` untrustworthy (the root guards walk into `.claude/worktrees/` and double-count the tree; `.gitattributes` covers no golden so `defense_messages.golden` fails on Windows and passes in CI). Task 1 gives `itemvoices` the picker seam and golden it has never had, built from PRE-migration code, because a golden written after the migration proves nothing. The three migrations then require every golden to come out **byte-identical**, and the plan forbids `-update` outright, since `defense_messages.golden` keys rows by the authored role name and is the only thing that can catch a role swap. Records the one piece of bug-compatibility easiest to optimise away by accident: `RenderTriad` calls `pick(n)` even when an index override is supplied, so the draw is discarded rather than skipped, and skipping it would consume one fewer `util.Rand` and shift every subsequent draw in the process. Two sabotage probes are mandatory, each verified to compile before being trusted |
| [`superpowers/specs/completed/2026-09-11-messaging-m3-item5a-narration-defects-design.md`](superpowers/specs/completed/2026-09-11-messaging-m3-item5a-narration-defects-design.md) | Design for the first half of messaging M3 item 5. The arc spec called item 5 "conditions, spells, quests: the Actee slot appears here", and reading the source showed that framing is backwards for conditions: a condition's "user" line already goes to the holder, so the missing viewpoint is the caster, and the condition event carries no caster id. Item 5 turned out to be six live defects sharing one cause, so the owner split it into 5a (fix the defects on today's paths) and 5b (migrate onto the narration core as a pure refactor). The six: condition room lines and quest room text go out on the audio channel although their text is visual, so they reach blind observers; spell conditions on a player are narrated twice to every audience; mob condition trigger text never fires; quest 77 sends a literal `{source}`; and 21 quest lines never say who acted. Records that M2's condition room line, added because the M1 audit called that case a gap, doubled the room line for 5 conditions, since condition start text already narrated the room via an event neither followed. Owner rulings: the merged who-plus-what line for spell conditions moves to M6 and 5a fixes only self-cast wording, which matters because area spells put the caster in their own target list; and the 21 quest fragments are rewritten to start with `{source}` now, backed by a startup check that fails the boot on a quest line without it. Includes a playtest plan staged in real rooms, and states plainly that the mob trigger fix can be checked only by unit test |
| [`superpowers/specs/completed/2026-09-11-followup-slice-a-names-in-dark-design.md`](superpowers/specs/completed/2026-09-11-followup-slice-a-names-in-dark-design.md) | Design for follow-up slice A of messaging M3 item 5a, the first of five follow-ups (A, F, B, C, D+E). Two 5a findings, widened by reading the source: a player could cast at someone they could not see and the target was told the caster's name, and the SWEEP crit counter named both combatants to players in the dark on the audio channel. Every "happens to you" line that names the other party ignored sight, and hidden creatures could be named by every command that names one, so `look <hidden creature>` gave them away. The narration seam learns who is who and hides names per viewer ("Something", or "a figure" for infrared); spell target, defence, mob cast, Purge Affliction and all three counter paths move onto it. Targeted casts need sight, infrared aims at `shape` / `2.shape`, and one rule decides who you perceive for the room listing and for every player command that names a creature, with staff tools exempt. Records that "Also here" required a pet as well as see-hidden, a leftover from upstream commit `463a76727` that moved see-hidden off pets; no player can own a pet in dogmud, so the room listing never showed a hidden creature to anyone, and the owner dropped the pet check. Player-facing only: mobs perceiving darkness is slice F |
| [`superpowers/plans/completed/2026-09-11-messaging-m3-item5a-narration-defects.md`](superpowers/plans/completed/2026-09-11-messaging-m3-item5a-narration-defects.md) | Implementation plan for the M3 item 5a spec above, in 10 tasks. Each defect is fixed test-first on its existing path: self-cast wording in four spell cases, condition room lines moved to the visual channel with the mob holder given the mob colour, trigger text sent from the mob tick, and quest room text substituted and sight-gated. A startup check that fails the boot on a quest line without `{source}` is built BEFORE the 21-line data rewrite and wired into boot only after it, because the shipped data fails the check today and one hooks test loads real quest data. Records the corrections to the spec found while planning: the Cat's Eye Draught the darkness lanes depend on grants no night vision (its condition misspells the flag), so a one-line data fix with a test is folded in; the D1 playtest actor must be `veteran`, since Conviction Surge's condition has no room line to silence; the lanes need three short runs in three far-apart rooms; the dark quest lane must alternate who triggers the once-per-character quest; no target is named in the dark; and self-cast lines keep the crit tag. States the traps an executor would otherwise hit: delivered lines keep their ANSI tags, `countContaining` already exists, fixture room 2 is not dark, and seeding conditions replaces the whole registry |
| [`superpowers/plans/completed/2026-09-11-followup-slice-a-names-in-dark.md`](superpowers/plans/completed/2026-09-11-followup-slice-a-names-in-dark.md) | Implementation plan for follow-up slice A (names in the dark), from its spec. Seventeen tasks: participant sight and name hiding in `internal/messaging`; the narration seam hiding names per viewer and every existing Audience passing both names; the counter, crit (the SWEEP leak) and spell lines moved onto the seam; one `Perceives` rule for the room listing and a viewer-aware `FindByNameSeenBy`, passed by every player command that names a creature and enforced by a root registry guard; targeted casts refused without sight, with `shape` / `N.shape` aiming for infrared; a player's search ending a hider's hiding; cast help, docs, three playtest lanes and the ship gates |
| [`superpowers/specs/completed/2026-09-11-followup-slice-f-mobs-perceive-darkness-design.md`](superpowers/specs/completed/2026-09-11-followup-slice-f-mobs-perceive-darkness-design.md) | Design for follow-up slice F of messaging M3 item 5a, the sibling of slice A: mobs stop picking targets they cannot see, and the dark-dwelling species get their night vision back. Records that condition 29 is absent from dogmud entirely and is referenced by EIGHT species, not the two memory claimed, so 67 mobs of 641 have silently been blind in their own caves; that no player is affected because all eight are mob-only; and that mob decision code ignores light completely, proven with a control grep rather than an empty result. Finds that aiprofile is a mob YAML field selecting combat move preferences, not a sight surface, so the three surfaces are the behaviour tree, mobcommands/attack.go (a nil viewer) and mobcommands/cast.go (the same InitiateCast players use). Gates four hostile room scans but deliberately not the two quest-giver conditions (which would otherwise stop Dewey and Cleric Hadwen offering quests in an unlit room), nor the companion sweep, nor the archer melee check: those last two report an engagement that already exists rather than choosing a target, and gating them would contradict the ruling that fights already under way continue. Records that a light source cancels the darkness for everyone in the room, which is what keeps the Ironwind cave bosses attacking, and adds the boot guard that would have caught the missing condition on the day it broke |
| [`superpowers/plans/completed/2026-09-11-followup-slice-f-mobs-perceive-darkness.md`](superpowers/plans/completed/2026-09-11-followup-slice-f-mobs-perceive-darkness.md) | Implementation plan for slice F, in 9 tasks. Task 1 restores condition 29 BEFORE task 2 adds the boot guard, because that guard panics on exactly the dangling reference condition 29 is today, so wiring it first would panic the server on the data it exists to catch. One shared mobCanSee helper wraps the same predicate combat already uses, then five hostile room scans take it, the scout condition moves onto Perceives, and the two single hooks (a nil viewer in mobcommands/attack.go, a non-player early return in cast_admission.go) gain sight. Records the traps an executor would otherwise hit: probing mobCanSee by swapping CanSeeSightImpairedOnly for CanSeeClearly proves nothing because they differ only on sleep; mobcommands tests must set SpeciesId 1 or GetSpecies returns nil; and mobcommands/cast.go must keep building MobActor with its Room populated, since NewMobActor leaves it nil and admitCastAim returns early on a nil room, which would silently disable the whole gate |
| [`superpowers/specs/completed/2026-09-12-followup-slice-b-red-names-and-combat-name-leaks-design.md`](superpowers/specs/completed/2026-09-12-followup-slice-b-red-names-and-combat-name-leaks-design.md) | Design for follow-up slice B of messaging M3 item 5a, third in the ruled order (A, F, B, C, D+E). Three defects: nearly every name rendered in the bright red `aggro` colour because the suffix compared a combat target's UserId with the viewer id and both are 0 for a room broadcast about a character not fighting a player; the return-damage recoil lines named an unseen combatant in the dark, adjectives and all; and both copies of the retarget notice named the new target regardless of sight. Fixes the primitive (the suffix needs a real viewer, no call site changes), routes recoil through the narration seam as slice A did the crit lines, teaches the seam to take a formatted name's adjective span with the hidden tag, and gives the retarget notice one sight-aware builder |
| [`superpowers/plans/completed/2026-09-12-followup-slice-b-red-names-and-combat-name-leaks.md`](superpowers/plans/completed/2026-09-12-followup-slice-b-red-names-and-combat-name-leaks.md) | Implementation plan for slice B in six tasks: the aggro viewer precondition with its test, the adjective-span rule in `HideNames`, `emitReturnDamageText` onto `SendTrio` with dark, infrared and lit tests, one `actions.RetargetNotice` builder for the four retarget calls (the round driver's two and the mob-departure retarget's two, the latter found by review), the wait-round participant lines hidden per reader (found by the playtest), package docs and patch notes with the ship gates, and a playtest lane fighting the Stone Beetle Queen in the dark |
| [`superpowers/specs/completed/2026-09-12-followup-slice-c-buff-notices-design.md`](superpowers/specs/completed/2026-09-12-followup-slice-c-buff-notices-design.md) | Design for follow-up slice C of messaging M3 item 5a: every condition tells its holder when it starts and ends. Records that 46 of the 102 dogmud conditions carried neither a start nor an end line (the two sets are identical: potions, meals and most afflictions), so a potion printed only the item's drink line and then nothing. One door for the player line (`StartUserNotice` / `EndUserNotice` on the condition spec, authored text first, a generic "takes effect" / "has expired" line underneath, silence for a secret condition), a root guard that fails the build when a non-secret condition lacks authored text or a secret condition carries any, a boot warning for conditions relying on the fallback, authored lines for 40 conditions plus an end line for the quit meditation, two flag-declared silences (`hidden` never announces its end, `silent-start` leaves the start to the applying command), and three system conditions (respawn grace, the synthetic infrared condition, the alt-character gear lock) made secret so they also leave the `conditions` list. Two mechanisms the playtest and reviews forced in: the condition event carries a duration multiplier so scaled potion conditions still travel the one door (a second root guard forbids direct character-level player adds), and room mutator conditions are refreshed with `Conditions.RefreshCondition` instead of lapsing and re-narrating every few rounds |
| [`superpowers/plans/completed/2026-09-12-followup-slice-c-buff-notices.md`](superpowers/plans/completed/2026-09-12-followup-slice-c-buff-notices.md) | Implementation plan for slice C in seven tasks: the resolver methods on the condition spec with their tests, the apply and prune hooks reading them, the boot warning, authored start and end lines for 40 conditions with a per-condition table of what each line says plus three conditions made secret (corrected after the content pass, see the note in Task 4), the root guard proven red by removing one line, docs and patch note, and a playtest lane that drinks a Warrior's Brew and a Purging Draught and watches `conditions` |
| [`superpowers/specs/completed/2026-09-12-followup-slices-d-e-purge-target-and-flag-guard-design.md`](superpowers/specs/completed/2026-09-12-followup-slices-d-e-purge-target-and-flag-guard-design.md) | Design for follow-up slices D and E, the last of the 5a follow-ups. D: Purge Affliction's dispatch never consulted mob targets, so a cast at a charmed companion purged the caster; the hook now takes a player, a mob or the caster and narrates through the seam. E: the engine declares its flags but never validated a condition's, so a misspelled flag loaded silently (the Cat's Eye Draught shipped that way); `AllFlags` plus a load-time panic and a root test close it, `poison-immunity` on Stone Stomach becomes real at both condition primitives and the poisoned condition, and Savant's Infusion moves to the catalyst price tier |
| [`superpowers/plans/completed/2026-09-12-followup-slices-d-e-purge-target-and-flag-guard.md`](superpowers/plans/completed/2026-09-12-followup-slices-d-e-purge-target-and-flag-guard.md) | Implementation plan for slices D and E: the purge dispatch and hook with a mob-target test, the flag registry with a completeness test that parses the constants, poison immunity at the two primitives and the condition adder, the root flag guard proven red by a misspelling, the price change, docs and a playtest lane that purges a companion and drinks Stone Stomach |
| [`superpowers/specs/completed/2026-09-12-messaging-m3-item5b-kind-b-store-migration-design.md`](superpowers/specs/completed/2026-09-12-messaging-m3-item5b-kind-b-store-migration-design.md) | Design for messaging M3 item 5b, the second half of item 5: conditions, spells and quests migrate onto the narration core as a byte-identical refactor. Each store gains a phase selector, a `Narration` that assembles the roles (a condition's holder line is the Actee, by owner ruling, with Actor reserved for the caster M6 will author) and a `Narrate` door; one `textutil.Narrate` is the only way the three stores reach `Render`, and `SendPhaseText` is deleted. Records why the core gains `FirstPicker`: `util.Rand(1)` still draws from `math/rand`, so rendering roughly 750 single-string fields through the default picker would add a hidden random draw per narrated phase and shift every later combat roll, and the core cannot special-case a pool of one because itemvoices legitimately holds one-line pools. Three goldens are built from pre-migration code first and must come out byte-identical, with two sabotage probes and two new root guards. Name hiding for these stores stays with M4 and M5 as the arc places it |
| [`superpowers/plans/completed/2026-09-12-messaging-m3-item5b-kind-b-store-migration.md`](superpowers/plans/completed/2026-09-12-messaging-m3-item5b-kind-b-store-migration.md) | Implementation plan for the M3 item 5b spec above, in 13 tasks (0 to 12). Task 0 records three goldens from pre-migration code and is the only `-update` in the plan; Tasks 1 to 5 add `FirstPicker`, the `textutil` adapter and the three store doors; Tasks 6 to 8 rewrite the 11 `SendPhaseText` sites, the three silent-start appliers, the quest bridge and the reward hook; Task 9 deletes `spelltext.go` and switches the golden builders to the doors, requiring all ten goldens byte-identical; Task 10 adds the two root guards and registers the newly visible sites; Task 11 documents; Task 12 runs the gate, the lit lane A playtest against the verbatim 5a lines, and opens the PR |
| [`superpowers/specs/completed/2026-09-15-messaging-m3-item6-crafting-progression-design.md`](superpowers/specs/completed/2026-09-15-messaging-m3-item6-crafting-progression-design.md) | Design for messaging M3 item 6. Crafting becomes a store with a door (`crafting.Phase`, `RecipeSpec.Narration` / `Narrate` through `textutil.Narrate`): `success_message` and `failure_message` are the Actor lines, and two new empty keys, `success_room_message` and `failure_room_message`, are the Observer slot M6 will author (owner ruling: no Actee until enchanting another player's gear exists). Progression's six raw `events.Message` sends move onto the pipeline through a boot-registered `characters.SetProgressionNotifier`, because `messaging` imports `characters`; they land on `CategorySkillProgress`, which turns banners and untagged words gold (color 179), matching quest skill-up lines, by owner ruling. Crafting output is byte-identical, proven by a `crafting.golden` recorded first; new root guards forbid raw `events.Message` outside the pipeline plumbing and assert the notifier is registered in `main.go` |
| [`superpowers/plans/completed/2026-09-15-messaging-m3-item6-crafting-progression.md`](superpowers/plans/completed/2026-09-15-messaging-m3-item6-crafting-progression.md) | Implementation plan for the M3 item 6 spec above, in 11 tasks (0 to 10). Task 0 records `crafting.golden` from pre-migration code and is the only `-update`; Task 1 adds the recipe door and validation; Tasks 2 to 4 move the instant, multi-round and mob craft sites onto it; Task 5 switches the golden to the door and extends the store field guard with per-file exemptions; Tasks 6 and 7 add the progression notifier, its hooks callback and a boot-registration guard; Task 8 adds the raw `events.Message` guard; Task 9 documents and adds item 6's rows to the M6 content ledger; Task 10 runs the gate and a two-player playtest checking crafting silence to the room and the gold banner bytes |
| [`superpowers/specs/2026-09-15-messaging-m3-item7-gossip-tips-design.md`](superpowers/specs/2026-09-15-messaging-m3-item7-gossip-tips-design.md) | Design for messaging M3 item 7. Gossip templates move out of hook globals into an `internal/gossip` store, loaded and validated at boot, with every pick rendered through `narration.Render` at the same random draw count. The periodic broadcast is renamed from hints to tips all the way down (`tips.yaml`, an `internal/tips` store, `set tips` with `set hints` kept as an alias, renamed listeners) and migration 0.18.0 renames each player's saved on/off flag. Owner ruling: conversations are not migrated, because speaker A and B are two NPCs saying consecutive lines through `say`, not two audiences of one moment. Records that tips are unwrapped long lines (56 of 74 over 80 characters), filed for M5's wrap decision |
| [`superpowers/plans/2026-09-15-messaging-m3-item7-gossip-tips.md`](superpowers/plans/2026-09-15-messaging-m3-item7-gossip-tips.md) | Implementation plan for the M3 item 7 spec above, in 9 tasks (0 to 8). Task 0 records `gossip.golden` and `tips.golden` from pre-migration data and is the only `-update`; Task 1 builds the `internal/gossip` store with a one-draw picker test; Task 2 moves the gossiping NPC onto it and deletes the hook globals; Task 3 builds `internal/tips`, renames `hints.yaml` and the two listeners; Task 4 adds `set tips` with the `set hints` alias and help; Task 5 writes migration 0.18.0 and bumps the version; Task 6 adds the one-owner data-file guard; Task 7 documents; Task 8 runs the gate, a tips and gossip playtest, and the PR |
| [`superpowers/specs/2026-09-16-messaging-m3-item8-combat-messages-design.md`](superpowers/specs/2026-09-16-messaging-m3-item8-combat-messages-design.md) | Design for messaging M3 item 8, the largest store in the arc at 6,629 authored lines and the third live instance of the defect the core exists to prevent: combat narrates one swing to three or four audiences by taking an independent `util.Rand` draw per audience, so attacker, defender and room are routinely told about different events. The `msgSeed` path that looks like coordination is dead in production (`ConsistentAttackMessages` ships `false` while `configs/context.md` documents `true`) and would not have coordinated anyway, because one seed hit `% len` against four different pool lengths. Records that the pools are authored as index-paired triads that have drifted apart: 446 of 534 per-tier role groups disagree and only 4 groups have all three roles equal, so the core would render silence. Owner ruled padding over trimming, so the slice authors 984 lines to per-tier equality before moving the store onto `narration.Render`, with `Together` and `Separate` mapping onto the four roles `render.go:35-38` was written for. Adds the validator the defence sibling has always had and the attack store never did, and deletes `ConsistentAttackMessages` outright rather than repairing it. Traces that knob to upstream GoMud (`2a1c51087`, Volte6, 2024-11-21), where it worked because upstream pools are equal, so one `ItemId` seed gave a coordinated triad and per-weapon voice at once; Stage 9.2 (`bcd08700b`) then expanded pools and switched it off in the same commit, which is the exact moment per-role independent draws and the uncoordinated triad were born, and Stage 9.5 added tiers the next day so flipping it back would no longer coordinate. Re-shapes the golden from "index 0 per pool" to 2,280 coordinated rows covering all 6,975 lines, closing a gap where a dropped or reordered line passed green. Records that coordination drops the draw count from three or four to one, which shifts the global random stream and must be expected rather than debugged. Measures `world/default` as vestigial across 30+ subsystems with `ValidateWorldFiles` checking directory names only, so it is commented as a GoMud holdover and filed, not fixed; delivery stays on `AttackResult` buffers for M4 |
| [`superpowers/plans/2026-09-16-messaging-m3-item8-content-pad.md`](superpowers/plans/2026-09-16-messaging-m3-item8-content-pad.md) | Implementation plan for PR 1 of the item 8 spec above, in 7 tasks (0 to 6). Task 0 builds `tools/combat_message_pool_audit.py`, a read-only reporter whose 984-lines-across-446-groups output is the red signal; Task 1 widens the snapshot golden from index 0 to every index and records it as a tagged baseline BEFORE any content, which is what makes every later commit provably additions-only, and also records `coupdegrace` for the first time since the builder's 8-entry intensity list omitted it; Task 2 pads `slashing` as the reviewed sample and stops for owner acceptance of the pairing; Task 3 is the per-weapon procedure applied to the remaining 19 files in a fixed order, `generic` first because it is the fallback every subtype reaches, including `shooting`'s two nulled `todefenderroom` roles; Task 4 verifies the whole store, the 6,978-row golden and that only 6 pre-existing rows changed; Tasks 5 and 6 are docs, the ledger and the PR. Records that the audit tool must never write YAML, since `yaml.dump` would destroy the token comment header and the quoting in every file |
| [`superpowers/plans/2026-09-16-messaging-m3-item8-core-migration.md`](superpowers/plans/2026-09-16-messaging-m3-item8-core-migration.md) | Implementation plan for PR 2 of the item 8 spec above, in 11 tasks (0 to 10), which cannot start until PR 1 merges because the new validator fails the boot on 446 groups until the pad lands. Task 1 adds `PoolFor` and one `Render` per split with the Actor/Actee mapping spelled out, since three same-shaped pools becoming adjacent struct fields is how the defence store nearly shipped an inversion; Task 2 pins one random draw per message, down from three or four; Task 3 rewrites `Validate` to delegate to `narration.ValidateVariants` per (intensity, split, tier) with `minVariants` 1 rather than defence's 5, because 414 of 534 shipped groups hold fewer than five; Tasks 4 and 5 collapse the two combat call sites; Task 6 deletes `ConsistentAttackMessages`, `msgSeed`, both seeded getters and the unreachable `if seedNum[0] == 0` branch, with an anti-backslide guard probed by a commented-out occurrence; Task 7 re-keys the golden to 2,283 coordinated rows and proves the message multiset unchanged; Task 8 comments the default tree; Tasks 9 and 10 are docs and the gate. Every probe is verified red before its green is trusted |
| [`superpowers/specs/2026-09-16-messaging-m3-item9-weather-emotes-design.md`](superpowers/specs/2026-09-16-messaging-m3-item9-weather-emotes-design.md) | Design for messaging M3 item 9, weather emotes, the last store in M3 and the only actorless one: it joins the renderer, tokens and pipeline while keeping its shape, rendering through `narration.Render` with `Observer` alone rather than pretending an ambient line has an Actor. `modules/weather/content` becomes the first `modules/` package to import `internal/narration`, which is cycle-free because that package imports stdlib only. Records that all 31 shipped indoor lines assume a built structure (roofs, eaves, floorboards, nails, rafters, panes) while `indoor` resolves to `default` for every indoor biome, so 124 of the 161 indoor rooms are underground and receive house prose, and that several indoor lines narrate the weather outside rather than what is felt inside. Owner ruled a three-way outdoors/indoors/underground split as a third section in the weather schema, with `Table` embedding `TableSection` inline so the seasonal variants and seasonal-ambience tables inherit it rather than the field pair being duplicated; classification lives in two Go maps beside the tables, not a new YAML load path and not derived from `indoor && darkarea`. `spiderweb` disproves that derivation and corrected the first pass of the design: it is indoor and dark but its darkness is webbing rather than stone, and it has zero rooms, so it is classified surface indoor with no prose authored. Class resolves from the `(biome, indoor)` pair `EmitAmbient` already passes, so the emitter and the whole sim side are untouched. The validator wraps `ValidateVariants` because weather is the only store where silence is a legal authored value (`mild: []` means light weather is inaudible through walls), giving the rule "empty by intent, or at least 6". Full content pass to depth 6 across all three sections, roughly 234 lines. Four shipped-data guards close the biome coupling a hardcoded list would otherwise leave unenforced, the load-bearing one being that every `indoor: true` biome must appear in exactly one of the two maps, so a newly added biome is a red build naming the choice rather than silent house prose in a crypt; it must itself be proven capable of failing. The golden does not exist yet among the 13 and is recorded from pre-migration code first, because one recorded after the refactor bakes the refactor's bugs into the baseline |
| [`superpowers/plans/2026-09-16-messaging-m3-item9-weather-core-join.md`](superpowers/plans/2026-09-16-messaging-m3-item9-weather-core-join.md) | Implementation plan for PR 1 of the item 9 spec above, in 5 tasks (0 to 4), a pure refactor with a byte-identical golden. Deviates from the spec's PR split on purpose: the three-way schema change moves to PR 2, because reclassifying `cave` as underground while that section is still empty makes `bandedSectionLines` correctly refuse to fall back, and 124 of the 161 indoor rooms go silent until the prose lands. Task 0 records `weather_emotes.golden` from pre-migration code and needs no `*With` variant the way itemvoices did, because weather already had a picker seam: `Pick` takes `roll func(int) int`, which is `narration.Picker`'s exact underlying type, so `SequencePicker` is assignable with no production change. The builder sweeps authored biome keys UNION a representative set including `cave` and `dungeon`, which is what makes the golden able to see PR 2 at all: the authored indoor keys are `default` only, so sweeping authored keys alone would leave the central change with no diff to inspect, and the row axis is named `sheltered` for the room rather than for the section it resolves to so the keys survive the split. Task 1 proves the golden capable of failing with a sabotage verified to compile under `go vet` first. Task 2 moves both `Pick` methods onto `narration.Render` with `Observer` alone, and records that `engine.EmitAmbient` needs no edit because the existing argument stays assignable |
| [`superpowers/plans/2026-09-16-messaging-m3-item9-weather-three-way-split.md`](superpowers/plans/2026-09-16-messaging-m3-item9-weather-three-way-split.md) | Implementation plan for PR 2 of the item 9 spec above, in 12 tasks (0 to 11), where the task ordering is load-bearing in two directions. The underground prose is authored BEFORE the class resolution is flipped, so the silence window never opens even between commits, and the depth validator is wired into the loaders LAST, because shipped pools are 1 to 4 deep and a minimum of 6 enforced earlier would fail every load. Task 0 embeds `TableSection` into `Table` inline so the per-season variants and the seasonal-ambience tables inherit the new class rather than `Table` carrying a duplicate field pair. Task 1 authors `rain` as the reviewed sample and stops for owner acceptance before the other eight, the item 8 procedure. Task 3 adds the two classification maps and rewrites `bandedSectionLines` to resolve class from the `(biome, indoor)` pair the emitter already passes, and pins the proof: every changed golden row must match `sheltered|cave|` or `sheltered|dungeon|` and nothing else. Task 4's guards found a live defect before they were written, so the plan carries the fix: `seasons/monsoon_wet.yaml` authors a `jungle:` key, `jungle` is a biome in neither world and no room uses it, so those two lines have never rendered and never could; Task 5 re-keys them to `forest` and adjusts the one phrase that names a jungle floor. Guard 1 is proven capable of failing with a throwaway `crypt` biome before it is trusted. Task 6 uses guard 4's red output as the padding worklist rather than a new Python tool, which would risk the `yaml.dump` trap that destroys quoting and comment headers. Task 10's playtest gate needs time in a zone rather than a fight that survives rounds, and carries the `config.yaml` skip-worktree warning for the cadence knobs it turns up |
| [`superpowers/plans/2026-09-17-messaging-m4b1-role-keys-and-loaders.md`](superpowers/plans/2026-09-17-messaging-m4b1-role-keys-and-loaders.md) | Implementation plan for the first half of M4b, in 9 tasks: canonical role keys (`actor`, `actee`, `observer`, `remote_observer`) in every shipped file and struct tag, a build-failing validation of every store against its shipped data, and the two-tier loader policy. The guards land BEFORE the renames, because a store whose struct no longer declares a tag loads its file silently empty, which is the exact failure the renames risk. Byte-identity cannot prove a rename, since golden labels legitimately change, so the proof is `tools/messaging_role_key_check.py`: the pre-rename golden with its labels translated must equal the new one, and a swapped role still shows as a diff where re-recording would bury it. Records that golden role labels sit in two different places depending on the store, inside the row key for defence, conditions and crafting but inside the value for combat, so the translation handles both. Taunt, grapple and position_control move into the boot-failing tier, and the latter two stop loading lazily from a hardcoded dogmud path, which is what made their failure invisible; `position_control.yaml` moves under the world tree, which dissolves the M0 guard's blind spot rather than patching it, and both messaging files are copied into the default world so the event tier's missing-file panic cannot strand a boot there. Conditions keeps `actee` rather than `actor` because a condition happens to its holder, the same asymmetry M4a's token flip recorded. Flags the key rewrite as anchored to key lines, since `self` and `room` are ordinary words inside grapple and position prose, and orders every table longest-first so `toattackerroom` cannot become `actorroom`. Corrects a survey false negative inherited from the M4 design work: `SpellData.TargetDefenseType` does have three live readers |
| [`superpowers/plans/2026-09-17-messaging-m4a-one-token-engine.md`](superpowers/plans/2026-09-17-messaging-m4a-one-token-engine.md) | Implementation plan for M4a of the spec below, in 9 tasks, leaving `narration.Substitute` the only token engine and `{actor}`/`{actee}`/`{actor_plain}`/`{actee_plain}` the only name vocabulary. Each store's Go token map and its shipped YAML flip in the SAME commit, because a map emitting `{actor}` against YAML still saying `{source}` renders the literal token to players; the grouping follows which Go vocabulary a store shares, so Kind B (conditions, spells, quests, crafting) moves with `textutil.TokenContext`, and combat, defence and taunt move with `items.TokenName`. Task 1 builds the rewrite tool with a per-store table and a dry run, doing text-line edits with a temp-file swap and never a yaml round trip, and excludes `templates/` and `users/`, whose `{target}` belongs to the unrelated prompt engine. Tasks 4, 5 and 6 delete the three surviving engines (`items.SetTokenValue`, `grapplemessaging.RenderTemplate`, `hooks.substitute`) as their store flips, and Task 6 first records a golden for `position_control.yaml`, the tenth store, which had none because it lives outside the tree the M0 guard walks; its stamina warning is the one asymmetric line, addressed to the character rather than the controller, and the golden pins it. Records that the conditions call sites cannot be fixed mechanically because the holder is the actee and no golden can see a call site at all, so `ConditionSpec.Narrate` takes the holder as two strings and owns the slot instead. Task 7 makes unknown tokens fail the boot for event stores, crafting having had no token validation of any kind, and Task 8 adds the AST guard the arc promised in M0 and never built, proven capable of failing with a sabotage verified to compile first |
| [`superpowers/specs/2026-09-17-messaging-m4b2-axes-notes.md`](superpowers/specs/2026-09-17-messaging-m4b2-axes-notes.md) | Superseded by the M4b-2 spec below. Working notes, not a spec: the session-end record of the M4b-2 design conversation, so the next session starts from the conclusions rather than rediscovering them. Reframes the M4 spec's "one DefenseType" as wrong on its own terms, because three concepts share that type and the five defence names are declared four times, including `combat.DefenseType` which no survey had counted and which stops at the three physical defences. Records that `AttackChannel` is not an axis but two axes flattened, which is why melee physical allows parry and ranged physical does not: parry is gated by attack type and quell and defy by damage type. Sets out the owner's four-axis model (attack_type, damage_type, targeting, defence) with eligibility keyed by the attack-and-damage pair, retires spell `type:` because harm versus help is just `damage_type: non_harm`, and records that the leaf package cannot live in `characters` because `characters` imports `items` and not the reverse. Reasons the targeting axis through grenades, cleaves, volleys and area spells of each damage type and concludes it changes how many contests happen rather than what may roll in one, so eligibility is untouched; what it does change is the defence text, since six victims of one blast each read the single-target line today, and whether a counter is earned. Records that area attacks grant counters PER TARGET today, verified at the drain loop and the four spell counter-tier call sites, so disallowing them is a live change and a buff to area attackers. Splits the work: M4b-2 is vocabulary and routing with no behaviour change, and a new counters slice re-keys the pools to the defence that won, authors the missing per-defence text, gates area attacks out, and ends with one playtest covering all of it |
| [`superpowers/plans/2026-09-18-messaging-m4b2-axes.md`](superpowers/plans/2026-09-18-messaging-m4b2-axes.md) | Implementation plan for the M4b-2 spec below, in 12 tasks. The leaf package lands first with its parity tests and context.md; the derived pools are pinned against the three switches they replace before any of them is touched; the store key, the defence enum and the seam flip in three separate compiling commits, each letting the compiler enumerate the consumers of the type it deletes; spells flip in three commits (additive Go, YAML gains the new keys beside the old, consumers flip and the old keys are stripped) so every commit boots. Records a collision the spec did not foresee, `SkillMoveParams.Attack` already being the attacker's `AttackSide`, and names the travelling value's field `Shape`. Ships `tools/spell_axes_rewrite.py` with add, strip, check and Go-test modes, line edits with a temp-file swap. The two behaviour commits (core-drain, non-harm casts at mobs) are the last two code tasks, each with a call-site test, and the PR body names them. Ends with the sweep grep proven capable of matching on master before it is trusted empty on the branch. Execution found four defects in the plan itself, all fixed on the branch: a type-only counter pool would have moved charm off the defy pool; the rewrite tool's mid-line lookahead silently made a physical test spell mental; a harm spell could load with targeting self and never be castable; and the data flip alone had already removed the mob-side contest, so the second behaviour commit made explicit what Task 7 had done implicitly |
| [`superpowers/plans/2026-09-18-messaging-counters.md`](superpowers/plans/2026-09-18-messaging-counters.md) | Implementation plan for the counters spec below, in eight tasks. The pool conversion lands first; the primitive takes the winning defence with a parity test over every reachable cell of the matrix and an invariant test that a defensive crit always names its defence; the two behaviour changes (area attacks earn no counter, every defy crit counter-taunts) are their own flagged commits with patch notes and re-key the root line-number guard; the data commit renames melee to dodge, authors parry and block, re-tones defy, deletes ranged, regenerates the golden once and proves it with `tools/counter_pool_rekey_check.py`, sabotaged before trusted; docs and ledger; verification and PR; and one adversarial playtest whose fixture is a weak attacker against a strong arena opponent so that every attack type is decisively defended and each defence line can be read beside its counter line. Execution found, through review, that the plan's gate placement let a defy win bypass the single-target gate (fixed by giving `FireCounterTaunt` the shape), that `CounterResult.Shape` had no reader (deleted), and that two context files the plan did not list (`internal/actions`, `internal/hooks`) had been falsified by the deletions |
| [`superpowers/specs/2026-09-18-messaging-counters-design.md`](superpowers/specs/2026-09-18-messaging-counters-design.md) | Design for the messaging M4 counters slice. Counter pools are keyed by the defence that won (counter-dodge, counter-parry, counter-block, counter-quell, counter-defy; counter-ranged retires), parry and block get their own counter text, the counter primitive refuses any attack that is not single-target so area attacks earn no counter, and every defy crit answers with a counter-taunt, charm included. Opens with a facts table read from source, carries the full attack x defence x targeting matrix the owner asked for, names the two behaviour changes for their own flagged commits, and ends with one adversarial playtest that must quote each defence line beside its counter line |
| [`superpowers/specs/2026-09-18-messaging-m4b2-axes-design.md`](superpowers/specs/2026-09-18-messaging-m4b2-axes-design.md) | Design for messaging M4b-2, superseding the axes notes. Four authored axes (`AttackType`, `DamageType`, `Targeting`, `Defence`) declared once in a new leaf package `internal/combatvocab`, with defence eligibility keyed by the attack-and-damage pair and an `Attack` value that replaces `AttackChannel` at every call site. Records two findings that the notes got wrong: `DamageChannel` is a derived pool axis, not the damage type, because a physical spell is dodged but scales magically and toughens willpower, so it survives and two derivation functions replace three hand-rolled switches; and a help spell cast at a mob is contested today, so a heal on a companion can be "defended", backfire, and earn the companion a counter-swing, which makes the mob-loop shortcut a flagged behaviour commit beside core-drain rather than a byte-identical routing fix. Owner rulings of 2026-09-18: `thrown` is a real attack type that the throw command passes; `none` is the attack type of a non-harm cast and pairs with `non_harm` by validation; targeting travels with the attack, not the item. Spell `type` and `target_defense_type` retire for `attack_type`, `damage_type`, `targeting`, rewritten by a line-edit tool over both worlds, with display strings derived so the `spells` listing and help template print exactly what they print today. Seven guards, each proven capable of failing |
| [`superpowers/specs/2026-09-17-messaging-m4-flip-design.md`](superpowers/specs/2026-09-17-messaging-m4-flip-design.md) | Design for messaging M4, the flip, split by owner ruling into five slices rather than the arc spec's one commit. M4a leaves one token engine (three leftovers still substitute outside `narration.substitute`, and the promised "no second engine" guard never existed) and rewrites shipped YAML to `{actor}`/`{actee}`, resolving the conditions store's inverted `{source}` that names the holder. M4b rewrites role keys to `actor`/`actee`/`observer`/`remote_observer`, proven by label-translated golden equality rather than re-recording, adopts a two-tier loader policy (event narration fails boot, ambient warns), unifies `DefenseType`, and makes spell `target_defense_type` required with a `non-harm` convention; core-drain moves off its hardcoded melee channel in a flagged balance commit. M4c bands melee defence on margin plus defensive crit like every other path, and records that the arc's "six band systems" are two (`AgingPhase` is item potency). M4d makes `SendTrio` the one path with a fourth audience and one sight verdict. M4e moves all audited Go narration (143 registered sites plus 25 frozen special-move files) into YAML, netted per site against `fmt.Sprintf` of the old literal. Finds a tenth store the arc never listed, `_datafiles/messages/position_control.yaml`, outside the tree the M0 guard walks, and records that the arc's dual-location loader never happened and NightVision is now granted by species and Cat's Eye Draught |
| [`superpowers/plans/2026-09-20-attack-narration-band-knobs.md`](superpowers/plans/2026-09-20-attack-narration-band-knobs.md) | Implementation plan, landed before M4c by owner ruling because the two band functions share no code (attack narration reads damage magnitude, defence bands read contest outcome). Pins `items.GetAttackMessage`'s five-intensity mapping with a boundary test it never had, then moves its two feel dials, Normal (30) and Heavy (75) percent of expected damage, into `Balance.AttackBandNormalThresholdPct` and `Balance.AttackBandHeavyThresholdPct`, validated as a pair; the Critical (101) and Miss (0) boundaries stay hardcoded because `combat.attackMessagePct` pins them to the crit flag and the `***` banner |
| [`superpowers/plans/2026-09-20-messaging-m4c-defence-bands.md`](superpowers/plans/2026-09-20-messaging-m4c-defence-bands.md) | Implementation plan for M4c, in 7 tasks. Task 1 builds `tools/melee_band_census` and measures the old self-relative z-score rule against the new crit-plus-margin rule across five score matchups before anything changes (audit below). Task 2 records a 165-row golden driving the real production `sendDefenseMessages` path BEFORE the flip, the only proof capable of catching a wording change hiding inside a band move. Task 3 makes the Normal-band cutoff `Balance.DefenceBandNormalThreshold`, shipping at 0.5. Task 4 is the flip itself: melee reads a new `meleeDefenceMargin(best)` derivation (0 when floored) through a `defenceBand{crit, margin}` struct into `items.RenderDefenseMessage`, deletes `items.GetDefenseMessage` and its 2.0/0.5 z-score cutoffs, and deletes 93 melee-seam rows from `internal/narration/testdata/stores/defense_messages.golden` discovered to be byte-identical duplicates of that file's own store rows, unable to fail on a band change. Task 5 moves weather's felt cutoff to `Balance.WeatherStrongFeltThreshold`, threaded from the engine into `content.Tables.Pick`/`content.SeasonalTables.Pick` as an explicit parameter rather than read inside `modules/weather/content`, which `TestContentPackageStaysPure` forbids from importing `internal/configs`. Task 6 is this docs pass. Task 7 re-runs the census on the new rule, the full gate and the PR |
| [`superpowers/plans/2026-09-21-messaging-m4e1-mob-special-moves.md`](superpowers/plans/2026-09-21-messaging-m4e1-mob-special-moves.md) | Implementation plan for M4e PR 1a, in 14 tasks: the thirteen `internal/mobcommands` special-move files move their wording into a new `internal/movenarration` store, and `canSeeInDark` is retired across all twenty files that reference it. Built on one finding read from source: `messaging.SendTrio` ALREADY hides each party's name by the reader's three-tier sight verdict and the mob files already hand it the names, so their `canSee` branches and `Something ...` twins are a redundant two-tier reimplementation and are DELETED rather than migrated. Carries four owner rulings of 2026-09-21 (ragged player pools squared by authoring, `usercommands/shoot.go`'s missing darkness check fixed as a flagged behaviour change, grapple's prose in `internal/actions` pulled in, mob-first split) plus two for PR 1b (squaring is a UNION of distinct feels rather than a choice between readings; ESL-opaque idioms flagged for M6 rather than fixed inside a byte-identity slice). Records three defects the plan itself shipped and the net or source caught: `attack.go` makes zero `SendTrio` calls so deleting its branch would have leaked a mob name to a blind player, `shoot.go`'s `mobName` is PRE-TAGGED at `:49` so its lines take `{actor}` not `{actor_plain}`, and the YAML already bakes the damage ansi wrapper so the token value passes bare |
| [`superpowers/audits/2026-09-21-m4e1-site-inventory.md`](superpowers/audits/2026-09-21-m4e1-site-inventory.md) | The 182-row inventory M4e PR 1a's YAML was authored from and its byte-identity net generated against: one row per narration literal across the thirteen mob special-move files, giving file, line, outcome branch, event key, role, the Go format string verbatim, its argument list, and whether the row is an anonymous dark twin (deleted, not migrated). Separately lists the prose that is NOT the files' own (`defence.ToRoom`/`ToDefender`/`ToAttacker` via `moveDefenceLines`, `result.DisarmResult.*` and `result.CritFailure.*` from `internal/actions`, `triadDef`/`triadRoom`). Records that no mob file uses a `util.Rand` variant pool, verified as a negative by proving the same search finds the pools in `internal/usercommands/kick.go` |
| [`superpowers/plans/2026-09-21-messaging-m4e1b-player-special-moves.md`](superpowers/plans/2026-09-21-messaging-m4e1b-player-special-moves.md) | Implementation plan for M4e PR 1b, in 7 tasks: the twelve `internal/usercommands` special-move files join the store PR 1a built, filling the `actor` role that was empty on every mob event, and retiring the last twelve entries from `m2FrozenFiles`/`m2RoutingFiles`. Its census corrects five facts the PR 1a survey got wrong (`gore` and `pounce` each have a second, non-knockdown hit pool; `kick` has a third sub-moveset; not every `partial` is 2/2/1; not every `miss` is 3/2/1) and sizes the work at 59 new lines, 41 parallel-squaring and 18 union-authoring, against an earlier estimate of 90 to 120 that had extrapolated divergence from a single verb. Records that 7 of the 12 files carry variant pools and 5 carry none, verified as a negative rather than assumed |
| [`superpowers/audits/2026-09-21-m4e1-darkness-diff.md`](superpowers/audits/2026-09-21-m4e1-darkness-diff.md) | The deliberate, player-visible half of M4e PR 1a, recorded so a reviewer can judge it without booting the server: exact rendered strings for one shipped line at every sight tier, before and after. The thirteen mob files each hand-rolled a TWO-tier darkness branch choosing between a named sentence and a hardcoded anonymous twin; `SendTrio` already judged the same question at THREE tiers, so the branches and twins were deleted rather than migrated. The improvement is the middle tier: an infrared character in a dark room used to read the fully-blind word ("Something") and now reads the shapes word ("A figure"). Two accepted side effects ride along, the `combat-anon` colour tag and sentence-start capitalisation through `HideNames`' `atSentenceStart` |
| [`superpowers/plans/2026-09-22-messaging-m5-pr1-quality-pass.md`](superpowers/plans/2026-09-22-messaging-m5-pr1-quality-pass.md) | Implementation plan for M5 PR 1, in 6 tasks: the last bare `_plain` name leak, the `throw.go` audience that hides from nobody, widening the existing observer-identity guard to the conditions and special-move stores rather than building a second one, pointing the `internal/templates` tests at the world players actually read, retiring the last raw `CategorySubmission` sender, and one `context.md` inconsistency. Corrects the spec while planning: only ONE End-phase condition line can leak, because `SendTextVisualAsLit` judges against `litRoom{}` and the shapes tier is structurally unreachable there, so condition 1's bare token is safe by accident rather than by design. Threads names through both paths anyway and pins the structural reason, so a future shapes tier on the lit path fails loudly instead of converting every light condition into a live leak at once |
| [`superpowers/plans/2026-09-22-messaging-m5-pr2-wrap.md`](superpowers/plans/2026-09-22-messaging-m5-pr2-wrap.md) | Implementation plan for M5 PR 2, in 8 tasks, ordered so nothing wraps until the wrapper is correct: fix the test oracle first because `displayWidth()` counts bytes like the function it grades, then `WrapAnsi`'s scalar `openTag` becomes a stack so balanced nesting survives a break, then rune-counted width, then `shouldWrap` becomes an explicit allowlist on the `skipStages` idiom (44 categories admitted, 17 excluded, all 61 accounted for). Records three exclusions found by reading rather than by direction: `CategoryRoomDescription` is already pinned unwrappable by a regression test for the look command's minimap columns, `CategorySplash` carries ASCII art behind a comment that falsely claims the pipeline wraps it, and `CategorySkillProgress` is a banner. Its live-render golden measured that `WrapAnsi` destroys a table's column alignment WITHOUT changing its line count (494 bytes to 436, newlines unchanged), so a shape assertion would have passed while an inventory listing was ruined |
| [`superpowers/plans/2026-09-22-messaging-m5-pr3-crime-in-the-dark.md`](superpowers/plans/2026-09-22-messaging-m5-pr3-crime-in-the-dark.md) | Implementation plan for M5 PR 3, in 10 tasks: `crimes.WitnessesInRoom` returns a `Witnesses{Identifying, ShapesOnly}` split by `CanSeeClearly` then `CanSeeShapes`, which composes the sleep gate for free because those two predicates call `awake()` while the optics primitive beneath them deliberately does not. Changing the return type makes the compiler enumerate all five call sites, one more than the spec knew about (the revenge AI seeder). Corrects the design's knowledge rule: giving a shapes-only witness `RecordCrimeWitnessed` but not `RecordMet` would still leak, because both are keyed on `knowledge.PlayerSubject(userId)`, so the loop must iterate `Identifying` only; today a single clear witness opens the guard and writes player-subject knowledge for every witness in the room including blind ones. Task 1 exists because `(&rooms.Room{}).GetVisibility()` panics in the `crimes` test binary with no biomes loaded |
| [`superpowers/plans/2026-09-22-lighting-plan1-scale-and-bands.md`](superpowers/plans/2026-09-22-lighting-plan1-scale-and-bands.md) | Implementation plan for plan 1 of the graded lighting arc, in 7 tasks: the graded `LightLevel`, the normal observer's band thresholds, and every consumer of `GetVisibility` migrated, with no player-visible change at all. Task 1 records a parity golden over all 1386 shipped rooms (the plan said 1387; the real count is 1386) and four observer kinds BEFORE anything changes, because a golden recorded afterwards only proves the change is self-consistent. Corrects the spec: `look.go` uses the visibility integer TWICE, and its second use (`< 2`) gates seeing THROUGH an exit into the next room, so the migration preserves two thresholds rather than one and a single-threshold migration would have silently broken corridor-peering everywhere. NightVision and InfraredVision deliberately keep their flag shortcuts here, because the window model is a real behaviour change for them and doing both at once would make the parity guarantee unassertable. Task 6 sabotages the golden in both directions and writes down the blind spot it finds: the golden covers sight tiers but cannot see exit-peering. ⚠️ **As shipped that gap is CLOSED, not open**: Task 5 added `internal/usercommands/look_exit_visibility_test.go`, which asserts on `look.go`'s own player-facing refusal text and was proven to redden under the same sabotage, so the plan's "verified by reading and by the boot check instead" wording describes an earlier state of the work |
| [`superpowers/plans/2026-09-22-lighting-plan2-vision-windows.md`](superpowers/plans/2026-09-22-lighting-plan2-vision-windows.md) | Implementation plan for plan 2 of the graded lighting arc, in 9 tasks: the NightVision and InfraredVision flag shortcuts are replaced by the window model, where an ability moves where an observer's usable light band sits rather than granting sight outright. Strength rides the existing condition `Effects` vocabulary, which already supports per-instance magnitude so plan 5 can scale it from stat and skill, plus a new MAX aggregation mode because summing would let two abilities stack into a window wider than either grants; and on `MutationEffect.Value`, which has existed all along and is ignored for flag effects today. Records the facts that shaped it: `nightvision` has exactly ONE content path (the crafted Cat's Eye Draught) and `infraredvision` is granted by NOTHING, so the plan grants it to cave mobs rather than shipping a mechanism with no consumer. Exactly one cell of shipped behaviour changes, a nightvision holder in an unlit room going from full sight to none, so the parity golden moves for the first time in the arc and Task 6 proves the diff's SHAPE before re-recording rather than accepting a blanket re-record. Unlike plan 1 this plan owes a patch note |
| [`superpowers/plans/2026-09-23-lighting-plan3a-celestial-mechanism.md`](superpowers/plans/2026-09-23-lighting-plan3a-celestial-mechanism.md) | Implementation plan for the first of plan 3's four slices, in 12 tasks: the mechanism only, with the biome vocabulary, the 117 orphan rooms, the 477-room city pass and the transition notices deliberately deferred to plans 3b, 3c and 3d because biome plus city alone is about 340 files and CI lint goes red over 300. Introduces internal/lightscale, a pure leaf package holding Combine and Attenuate with no config and no globals, the discipline SightThroughWindow set in plan 2; a celestial term in internal/gametime derived entirely from WorldLatitude, where the sun is Absent below the horizon rather than zero so night needs no special case and the flat-topped midday comes free from the shape of sine; and biome skylight and lamp fields replacing the deleted darkarea and litarea booleans, both pointers because zero is meaningful for both and coercing a cave's zero sky to the default would make every cave as bright as an open field. Task 1 records the new day-cycle golden against the UNMODIFIED tree at twelve moments spanning the year and the day, and proves the recording is not fake, because a Go test binary has NightHours 0 and an unpinned baseline would record a world with no night in it. Task 10 proves four properties of the diff's shape before re-recording, and Task 11 sabotages the latitude, a sky fraction and the doubling step to prove the golden can fail at all. Retires the parity golden's guarantee explicitly, since it was plan 1's behaviour-preservation proof and this is a deliberate behaviour change. Also finishes the named lit predicate plan 1's design promised and never built: fifteen sites were copying a 424-field struct at a measured 99.75 ns to read one int, several inside per-round loops. Records that the per-round memo is NOT a performance measure, because the uncached computation is 0.005% of a round, so nobody grows it into the per-room cache the numbers never justified |
| [`superpowers/specs/2026-09-22-graded-room-lighting-design.md`](superpowers/specs/2026-09-22-graded-room-lighting-design.md) | Design for the graded room lighting arc, the immediate follow-up to messaging M5, split into six plans. Room visibility becomes a scale from minus 100 to 100, and the load-bearing idea is that a vision ability moves the observer's usable window along that scale rather than adding light to the room: nightvision shifts the window left by its strength and is therefore dazzled in daylight, while infravision carries a second number for how far below zero it still senses heat, which makes magical darkness definable as the point past its reach. Records that the migration is cheap because 12 of the 14 consumers of `GetVisibility` only ask whether the room is lit, that the three moons already exist with smooth phase contributions and are already load-bearing for combat stats and bloom gating, and that seasons and weather must keep reaching rooms through mutators because modules cannot be imported by `internal`. Derives the moons' relative brightness from their lore distances and sizes rather than inventing numbers, and honours `world.md:70`'s constraint that true darkness is rare by making a moonless night the only outdoor case that blinds a normal person. Sources stack with halving diminishing returns so lamps converge near twice the brightest rather than brute forcing a darkness spell |
| [`superpowers/specs/2026-09-23-graded-room-lighting-amendment-celestial.md`](superpowers/specs/2026-09-23-graded-room-lighting-amendment-celestial.md) | Amendment to the spec above, written for plan 3 and superseding six of its decisions. The composition rule changes from halving diminishing returns to a single log-combine over every term including ambient, because the halving rule gives a lantern-lit street at noon a light of 125 and would put every lit city room past the dazzle edge all day; one knob, the doubling step, then governs stacking, the sky fraction and the daylight curve alike. The seasonal noon peak table and its season-mutator delivery are deleted before being built and replaced by real solar geometry at `WorldLatitude` 46.5, which produces day length and noon height from one input, removes the flatness exponent, and removes the sun's special case at night because `log2(sin altitude)` is simply absent below the horizon. Records that this takes the annual mean night from the shipped flat 8 hours to 12, and that natural daylight can then never reach the dazzle band. The biome `darkarea`/`litarea` booleans are deleted for a per-biome sky fraction and per-room lamp value, because those booleans hold 62% of the world permanently lit and cannot express 'dim gold' against 'street lantern'. Corrects the spec's claim that zero is the absence of light, which is incoherent on a log scale. Measures rather than assumes: the moon distribution over 2000 game days, the per-room sight outcomes over a full year, and the cost of the new math, which turns out to be 0.005% of a round so the per-room cache the design first called for is not built. Finds three defects on the way: weather renders inside the New Plymouth sewers, five zones name a biome that has no file, and plan 1's promised `IsLit` predicate was never built so fifteen sites copy a 424-field struct to read one int |
| [`superpowers/specs/2026-09-23-lighting-plan3b-biome-vocabulary-design.md`](superpowers/specs/2026-09-23-lighting-plan3b-biome-vocabulary-design.md) | Design for plan 3b of the graded lighting arc, which gives the world a biome vocabulary honest enough to use the mechanism 3a built. Six new biomes and one deletion: `sewer`, `interior` absorbing `house`, `dense_forest` at sky 0.25, `plains`, `river`, and `ether` for places outside the world. Records that the 117 orphan rooms are two different problems, because 81 of them are `a_dark_forest`, a zone nothing exits into, with zero mob spawns, stub descriptions and no reference anywhere in the content tree, so it is deleted rather than biomed. Corrects an earlier claim that five zones ask for `plains` and `river`: every room in all five already carries an explicit biome, so those declarations are dead config and the two biomes earn their place on world-building grounds only, changing no light. Finds two places labelled as what they resemble rather than what they are, a spacecraft command deck and a spider lair both biomed as cave, and finds that `spiderweb` has been a zero-room biome whose own comment describes exactly the twelve Foldweave rooms it never received. Derives `dense_forest`'s 0.25 from the observation that `forest` is already blind at night, so a darker forest can only differentiate at midday, and 0.25 makes the deep timber hide faces at winter noon while reading clear the rest of the year. States the onboarding constraint as a hard one: `look.go` refuses a room description below the blind threshold, so if the six character-creation rooms followed the sun a share of every day's new players would arrive unable to read their own introduction |
| [`superpowers/plans/2026-09-23-lighting-plan3b-biome-vocabulary.md`](superpowers/plans/2026-09-23-lighting-plan3b-biome-vocabulary.md) | Implementation plan for the spec above, in 10 tasks, almost entirely data: BiomeInfo and Room already carry skylight and lamp from 3a, so the only Go edits are two weather classification maps, a climate archetype binding and the biome template. Task 1 builds tools/lighting_golden_diff.py first, because six later tasks each move a golden and in 3a every implementer wrote that analysis from scratch while one reported a shape that was right for the wrong reason; the tool is proven both ways before use, reporting every section unchanged on an unmoved golden and catching a single perturbed room. Task 2 deletes a_dark_forest only after re-running the three greps that justify it, and expects a room count drop with ZERO rooms moved, since deleting rooms must not change the light of a room that remains. Task 4 proves biome_coupling_test.go can catch the house deletion before fixing it. Task 8 is the strongest check in the plan and expects NOTHING to move, because plains and river carry the same sky as land and water, so any change means a biome is authored wrong. Task 6 adds a permanent test that the six character-creation rooms are never dark, and proves it can fail by zeroing ether's lamp, because look.go refuses a room description below the blind threshold and a new player who cannot read their own introduction is a failure nothing else in the suite would report |
| [`superpowers/specs/2026-09-22-messaging-m5-quality-pass-design.md`](superpowers/specs/2026-09-22-messaging-m5-quality-pass-design.md) | Design for messaging M5, three PRs sliced by risk: the quality punch list, the wrap decision, and crime in the dark. Its 36-row facts table overturns four things earlier arc documents assert. There is no production `world/default` template shadowing at all (one path, no fallback); the defect is that the whole `internal/templates` test binary is pointed at the world no player reads, and flipping it breaks exactly two tests. Of the 17 bare `_plain` condition leaks, 15 were already fixed on 2026-09-21, leaving two End-phase lines plus one undocumented leak in `throw.go`, whose `Audience` carries `ActeeName: NoName` while the event renders a real mob name. `WrapAnsi` is broken today in two reproduced ways (a scalar `openTag` that any close clears, so balanced nesting loses color across a break, and byte-counted width whose test oracle shares the bug), so the owner's category-keyed wrap ruling gains a prerequisite repair commit; `CategorySystem` and `CategoryBroadcast` turn out to carry both free prose and pre-formatted tables, banners and the ASCII map, so neither may ever join the allowlist. The crime gate reads `CanSeeClearly` then `CanSeeShapes` rather than the raw optics primitive, which composes the owner's sleep gate for free and maps the shapes tier onto the `PerpUnknown` that `crimes.go` already returns for no witnesses |
| [`../tools/move_narration_net.py`](../tools/move_narration_net.py) | Extracts every special-move narration literal from the thirteen mob command files into `internal/mobcommands/testdata/pre_migration_literals.json`: the format string, its ordered argument list, and the verb/event/role it maps to. Feeds `TestMigratedWordingIsByteIdentical`, which asserts the YAML event renders exactly what `fmt.Sprintf` of the original produced, so a migration that moves a single character fails. It found two real defects before any production file was touched: `bash` dropping a `yellow-bold` wrapper on four lines, and `shoot` using a bare token where its name arrives pre-tagged |
| [`superpowers/specs/completed/2026-09-12-conditions-unification-slice-1-model-design.md`](superpowers/specs/completed/2026-09-12-conditions-unification-slice-1-model-design.md) | Design for slice 1 of the conditions unification arc (owner ruling 2026-09-12: the data-driven records absorb the ten combat conditions and the unified thing is called a condition; model first, then the Go rename, then the YAML keys and saves). The condition instance gains a per-instance magnitude and the spec a closed `effects` vocabulary of seven keys that combat reads through one door; the ten conditions become YAML records with the exact numbers their producers set today; the enum, its tick, the mirror flag and both shield decay helpers are deleted; one display list and one GMCP payload. Records that Minor Shield decays twice per round in combat today and that the dodge-penalty blinded condition has no producer anywhere. Scaling stays in the appliers (the spell scaling arc owns the formulas); a side-by-side equivalence test pins every live number before the old system is removed |
| [`superpowers/plans/completed/2026-09-12-conditions-unification-slice-1-model.md`](superpowers/plans/completed/2026-09-12-conditions-unification-slice-1-model.md) | Implementation plan for the slice 1 spec above, in 16 tasks (0 to 15). Task 0 pins every live number against the old enum with literal expectations that later tasks may not change; Tasks 1 to 3 add the magnitude, the effects vocabulary, the `Effect` door, the `AddConditionMagnitude` writer (an exact integer TRIGGER COUNT, never a multiplier: `3.3 × 10` truncates to 32; for the three-round dot and bleed records a rounds duration is converted by `conditions.TickTriggers` first), the seven records and the one golden re-record; Tasks 4 to 10 migrate one condition each, producer, consumers and tests together; Task 11 deletes the enum and lets the compiler enumerate; Tasks 12 to 13 collapse display and GMCP and add the timed-state guard; Tasks 14 to 15 document, playtest and ship |
| [`superpowers/specs/completed/2026-09-14-conditions-unification-slice-1b-mechanics-design.md`](superpowers/specs/completed/2026-09-14-conditions-unification-slice-1b-mechanics-design.md) | Design for slice 1b of the conditions unification arc: the behaviour changes the owner ruled on after slice 1, landed before the slice 2 rename so the rename carries no behaviour. Bleeds become stacking records (each application its own timer, one summed tick per round, count shown as "Bleeding (3)") with their numbers moved to fifteen `config.yaml` knobs; the spell dot ticks every round; the Recovering swing cap bites for players by moving `AttemptRecovery` after the condition tick; secret records leave both `Char.Conditions` and the `conditions` command through one `ConditionSpec.Listed` predicate (so `VisibleNameDesc` is deleted); `TickTriggers` is deleted |
| [`superpowers/plans/completed/2026-09-14-conditions-unification-slice-1b-mechanics.md`](superpowers/plans/completed/2026-09-14-conditions-unification-slice-1b-mechanics.md) | Implementation plan for the slice 1b spec above, in eight tasks: the fifteen bleed stack knobs (with the measured starting numbers: at Strength 100 each stack lasts 8 to 12 rounds for 2 to 3 health a round, 2 to 2.5 times the single hit it replaces, and a config test holds them to the slice targets), the stacking primitive in `internal/conditions`, the spell dot every round, bleeds as stacks from the knobs (deleting `TickTriggers`), the Recovering cap moved after the player condition tick, one `Listed` predicate and `DisplayName` for both condition lists, documentation, and the gate with a playtest |
| [`superpowers/specs/completed/2026-09-14-conditions-unification-slice-2-rename-design.md`](superpowers/specs/completed/2026-09-14-conditions-unification-slice-2-rename-design.md) | Design for slice 2 of the conditions unification arc: rename the old Go names to conditions in everything compiled or shown, with no behaviour change and nothing changed on disk or on the wire (owner disk/wire rule: YAML keys and values, data folders, config keys, saves, GMCP fields and messaging category strings wait for slice 3). Literal name swap (`conditions.ConditionSpec` to `conditions.ConditionSpec`). Layered and compiler-driven with `gopls rename`, opened by a wire-freeze test with template goldens, closed by an anti-backslide identifier guard. Flags the two traps: untagged fields whose yaml key comes from the Go name, and templates that read Go field names at runtime |
| [`superpowers/plans/completed/2026-09-14-conditions-unification-slice-2-rename.md`](superpowers/plans/completed/2026-09-14-conditions-unification-slice-2-rename.md) | Implementation plan for the slice 2 spec above, in eight tasks: freeze the wire (tags, key tests, GMCP JSON names, template renders), move the package (`gopls rename` of the package clause then `git mv`, verified on Windows), rename every identifier with a scratch `go/parser` scanner driving `gopls rename` by byte offset, then files, guard patterns and comments, player and admin text (admin command `setcondition` with `condition` as an alias), documentation, an identifier guard, and the gate |
| [`superpowers/specs/2026-09-15-conditions-unification-slice-3-disk-wire-design.md`](superpowers/specs/2026-09-15-conditions-unification-slice-3-disk-wire-design.md) | Last slice of the conditions arc: renames every old spelling a file, client or content author reads (YAML keys and values, data folders, GMCP builder fields, category strings, colour aliases, template functions) through one word map (`internal/conditionrename`), and migrates every save with a 0.17.0 startup sweep that renames old keys anywhere under DataFiles. Records that items save a full spec copy under `overrides:` in nearly every store (users, alts, storage, room and mob instances, shops, guild vaults, crates, auction data), that the 0.14.0 player sweep silently skips alts files, that every loader ignores unknown keys, and that the three old config knobs are dead (deleted); deletes docs for a scripting layer that does not exist |
| [`superpowers/plans/2026-09-15-conditions-unification-slice-3-disk-wire.md`](superpowers/plans/2026-09-15-conditions-unification-slice-3-disk-wire.md) | Implementation plan for the slice 3 spec above: word map package, admin alias removal, one atomic rewrite with an equivalence proof and a key-binding test, migration 0.17.0, config knob deletion, docs sweep and a string and data guard. Its Task 4 code is the superseded first migration; the spec's section 2 records the design as built |
| [`superpowers/specs/completed/2026-09-07-ranged-one-verb-design.md`](superpowers/specs/completed/2026-09-07-ranged-one-verb-design.md) | Ranged redesign: `fire` resolves the shot and chambers the next round as one action, retiring the player `reload`. Removes a two-way cooldown collision (reload denied an ambush; an ambush then stranded an empty weapon for four rounds) and rewrites quest 50, which existed to teach the loop being removed |
| [`superpowers/plans/completed/2026-09-07-ranged-one-verb.md`](superpowers/plans/completed/2026-09-07-ranged-one-verb.md) | Implementation plan for the ranged spec above, in 6 tasks. Folds chambering into `actions.ExecuteFire` so mob parity comes from the shared seam, flips `fire` to canonical with `shoot` as its alias, and pins the Go notify string to the quest YAML triggers with a contract test. Records one decision the spec left implicit: an ambush is no longer downgraded when the special-move timer is claimed, because the denial only ever existed to compensate for reload stealing it |
| [`superpowers/audits/2026-08-31-messaging-surface-sweep.md`](superpowers/audits/2026-08-31-messaging-surface-sweep.md) | M0 of the messaging arc: a mechanically derived inventory of every text-bearing key spelling and every pipeline bypass, locked in CI by `messaging_surface_guard_test.go` |
| [`superpowers/audits/2026-09-07-narration-viewpoint-audit.md`](superpowers/audits/2026-09-07-narration-viewpoint-audit.md) | M1 of the messaging arc: all 247 hand-rolled narration sites ruled for actor/actee/observer coverage. 240 are correctly incomplete (the actee is a mob, the target is a door, the line is a refusal); 7 are defects. Every defect is the same shape — a duplicated code path that copied the effect and dropped a viewpoint — which is the argument for M2's central seam. Records that the scanner's labels are wrong 26% of the time and must not be read as findings |
| [`superpowers/specs/completed/2026-09-08-context-budget-skill-extraction-design.md`](superpowers/specs/completed/2026-09-08-context-budget-skill-extraction-design.md) | Cuts the always-loaded context from approx 79 KB to approx 15 KB by extracting twelve on-demand skills into `.claude/skills/` from `CLAUDE.md` (1,094 lines) and `MEMORY.md`. A survey of all 102 `feedback*`/`reference*` memory files classifies them PROCEDURE/PREFERENCE/FACT/INCIDENT/LOG, and only the 64 PROCEDURE files fold. Records that a skill cannot cover a rule that fires unprompted, so the four hazards with incident history (the `gh` upstream-PR leak, python read-modify-write, `git add -A`, blanket server kill) become `PreToolUse` hooks instead; that several CLAUDE.md sections merely duplicate existing `docs/schemas/` files and should be deleted rather than moved; and that folded memory files are tombstoned, not deleted, because deletion would break 176 wikilinks across 73 files while buying no context tokens |
| [`superpowers/plans/completed/2026-09-08-context-budget-phase-1-skills.md`](superpowers/plans/completed/2026-09-08-context-budget-phase-1-skills.md) | Implementation plan for Phase 1 of the spec above, in 14 tasks: write the twelve skills, nothing deleted. Purely additive and reversible by `rm -rf .claude/skills`, which is why it is planned separately from the cut-over phase that can actually lose material. Carries the exact CLAUDE.md line range each skill lifts from (valid against commit `0e0f3acd9`) and the exact memory files each folds, so no task has to rediscover them. Task 0 builds the net first and proves it fails before any skill exists. Places the two PROCEDURE files the spec left unassigned, and records that `feedback_gh_defaults_to_upstream_fork_parent` exists twice, once underscored and once hyphenated |
| [`superpowers/audits/2026-09-08-skill-coverage.md`](superpowers/audits/2026-09-08-skill-coverage.md) | Task 13 of the Phase 1 plan above, closing Phase 1: proves the twelve skills are complete enough for Phase 2 to start deleting from CLAUDE.md. Confirms no memory file is double-claimed across skills bar the one disclosed `instance:"skip"` co-ownership, and mechanically diffs every lifted CLAUDE.md range word-for-word against its skill (all faithful once em/en-dash normalization is accounted for). The real findings are elsewhere: two skills' Sources sections omit ranges they actually lifted; several sections the plan expected to be "already covered elsewhere" turn out only partially covered (Regen System, Equipment Slots, Spell Duration, Condition/Ward, Inventory Disambiguation, Shop Persistence's pricing half); and the Salvage System section describes a chance formula `internal/crafting/context.md` says was deleted and replaced by a contest, so it must not be deleted as a duplicate without first writing its actual replacement. Also independently verifies CLAUDE.md's stale knob counts (352/466/1506 versus the real 375/508/2296) and several stale memory-file symbol references already caught and corrected inside the skills |
| [`superpowers/specs/completed/2026-09-08-context-budget-phase-2-design.md`](superpowers/specs/completed/2026-09-08-context-budget-phase-2-design.md) | Design for Phase 2 of the spec above: the phase that actually collects the token win, taking `CLAUDE.md` from 1,094 lines to roughly 71 and always-loaded memory from 33.3k tokens to roughly 7k. The Phase 1 audit changed its shape twice over: only 31 of 44 sections are safe to delete, and the Salvage section is not merely stale but actively wrong, naming three config knobs (`SalvageMinChance`, `SalvageMaxChance`, `SalvageSoftCap`) that nothing reads, where the live knob is `SalvageFloor`. So the phase is a mechanical deletion plus an authoring job: nine gapped sections get proper `context.md` sections written to the project convention, batched by destination, each deleted only after its replacement passes `tools/context_md_audit.py`. Opens with a step 0 that tests whether the twelve skills actually trigger on realistic prompts, because nothing has yet evidenced that assumption and everything else rests on it |
| [`superpowers/plans/completed/2026-09-08-context-budget-phase-2.md`](superpowers/plans/completed/2026-09-08-context-budget-phase-2.md) | Implementation plan for the Phase 2 spec above, in 13 tasks. Deletion is driven by `tools/strip_claude_sections.py` matching **exact heading text, never line numbers**, because removing one section renumbers everything below it, and the script aborts without writing if any manifest heading is missing. Records that all 44 headings are unique but four contain non-ASCII characters (`↔`, `—`), so the manifest is built by subtracting a verified KEEP list from CLAUDE.md's own headings rather than retyped. Task 1 routes fifteen realistic prompts through a blind judge that sees only the twelve descriptions, since nothing has yet evidenced the skills trigger. Every deletion step asserts **zero added lines**, because those steps are purely subtractive and a single addition means the script rewrote something |
| [`superpowers/audits/2026-09-08-skill-routing.md`](superpowers/audits/2026-09-08-skill-routing.md) | Task 1 of the Phase 2 plan above, and the evidence that plan's step 0 asked for: two blind rounds routing 25 prompts through a judge that saw only the twelve skill descriptions, zero tools, no expected answers. All 25 landed on the intended skill; round 1's apparent gap (`dogmud-combat` never won outright) was round 1 under sampling combat, and round 2's six combat-shaped probes all won it, clearing the finding. Fixes a real gap round 2 did surface, that the combat description never said "crit" despite crits being a named opposed-roll mechanic, by trimming wording elsewhere to add it (496 to 495 characters, cap 500). Records the combat/balance-config damage-scale overlap as accepted rather than fixed, since both descriptions sit near the character cap and the overlap routed correctly in both directions it was tested. Licenses the Phase 2 plan's Task 2 deletion of 31 CLAUDE.md sections on the strength of this evidence |
| [`perf/droplet-screenshots/`](perf/droplet-screenshots/) | DigitalOcean Insights captures per prod deploy, with an index explaining what each shows |
| [`aicompanion/implementation-plan.md`](aicompanion/implementation-plan.md) | Implementation plan for bonded AI companions: why they are built on the existing companion system rather than an AI-port account, the short engine touch list, the module architecture and locking model, phases 0 to 7 with acceptance criteria, and the cumulative patch process |
| [`aicompanion/settings.md`](aicompanion/settings.md) | Every AI companion setting, its default and what it does, and why the module ships no config overlay |
| [`aicompanion/upstreaming.md`](aicompanion/upstreaming.md) | What the AI companion asks a server to carry when it is switched off, and what to change before sending it upstream |
| [`aicompanion/testing-and-prompts.md`](aicompanion/testing-and-prompts.md) | What the AI companion's model is told on each request (and why that is enough), the fast/main/deep model tiers, the test plan including restart, copyover, outage and soak, and the lifelikeness checklist |
| [`aicompanion/feature-spec-v2.txt`](aicompanion/feature-spec-v2.txt) | Feature specification for the AI companion (version 2): ground rules and every feature by id (F1.1 onward), which the implementation plan and code comments refer to |
| [`superpowers/`](superpowers/) | Per-feature specs and implementation plans: live ones at the top of `specs/` and `plans/`, shipped work in `completed/`, and work that was dropped or superseded before it shipped in `abandoned/` |
| [`archive/`](archive/) | Retired documents and old bug screenshots |
| [`upstream/`](upstream/) | Upstream-facing material in both directions: artifacts inherited from the GoMud engine, and briefs for changes we want to send out to third-party tools |
| [`upstream/archify-sublabel-pr-brief.md`](upstream/archify-sublabel-pr-brief.md) | Ready-to-hand-off brief for a PR to `tt-a1i/archify`: four of its five renderers never measure `sublabel`/`tag` against the node box, so diagram text overflows while validation still passes |
| [`images/`](images/) | Screenshots used by the top-level README |

Per-package developer notes live beside the code, as `context.md` in each
`internal/` and `modules/` package — see the convention in the repo-root
`CLAUDE.md`.
