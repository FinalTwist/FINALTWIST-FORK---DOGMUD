moveid: grapple
events:
  success:
    actee:
      - '{actor} <ansi fg="yellow-bold">grapples</ansi> you, transitioning to <ansi fg="cyan">{position}</ansi> position!'
    observer:
      - '{actor} <ansi fg="yellow-bold">grapples</ansi> {actee} into <ansi fg="cyan">{position}</ansi> position!'
  fail:
    actee:
      - '{actor} tries to grapple you, but you slip away!'
    observer:
      - '{actor} tries to grapple {actee}, but fails!'
  crit_failure:
    actor:
      - '<ansi fg="red-bold">You overextend badly and fall to the ground!</ansi>'
    actee:
      - '<ansi fg="yellow-bold">Your opponent overextends and falls - you see an opening!</ansi>'
    observer:
      - '<ansi fg="combat">The failed grapple sends them sprawling!</ansi>'
  disarm:
    actor:
      - '<ansi fg="yellow-bold">You disarm {actee_plain}, loosening their grip on their {weapon}!</ansi>'
    actee:
      - '<ansi fg="red-bold">{actor_plain} disarms you! Your {weapon} slips from your grasp!</ansi>'
    observer:
      - '<ansi fg="combat">{actor_plain} disarms {actee_plain}, knocking their {weapon} loose!</ansi>'
  # player_* events are the PLAYER twin's own wording (M4e-1b, option B): the
  # twins shipped different prose for the same event, so they take separate
  # keys rather than sharing the mob events above. Merging them into one pool
  # is filed as M6 ledger row 56. (crit_failure and disarm above are already
  # SHARED between the mob and player call sites via
  # internal/combat/grapple_narration.go, so they need no player_ twin.)
  player_success:
    actor:
      - 'You <ansi fg="yellow-bold">grapple</ansi> {actee}, transitioning to <ansi fg="cyan">{position}</ansi> position!'
    actee:
      - '{actor} <ansi fg="yellow-bold">grapples</ansi> you, transitioning to <ansi fg="cyan">{position}</ansi> position!'
    observer:
      - '{actor} <ansi fg="yellow-bold">grapples</ansi> {actee} into <ansi fg="cyan">{position}</ansi> position!'
  player_fail:
    actor:
      - 'Your <ansi fg="yellow-bold">grapple</ansi> attempt against {actee} fails!'
    actee:
      - '{actor} tries to grapple you, but you slip away!'
    observer:
      - '{actor} tries to grapple {actee}, but fails!'
  # Private knowledge: no actee or observer line. A room line here would
  # invent an observation nobody in the room made, about a grapple the
  # player_success event already narrated to them.
  player_prone_penalty:
    actor:
      - '<ansi fg="yellow">{actee_plain} was already prone - they had little chance to resist!</ansi>'
  player_defense_exposed:
    actor:
      - '<ansi fg="red">Your failed attempt leaves you exposed!</ansi>'
